

PermitRootLogin yes
PasswordAuthentication yes
#not anymore PermitEmptyPasswords yes

When done:

# 1. Lock the root password
sudo passwd -l root

# 2. Edit the SSH config to prohibit root login
sudo vi /etc/ssh/sshd_config

useradd -m -G wheel david
passwd david

sudo rpm-ostree install cloud-utils-growpart --apply-live

sudo growpart /dev/mmcblk0 3
sudo partprobe /dev/mmcblk0
sudo resize2fs /dev/mmcblk0p3

#Verify partitions with
df -h

sudo systemctl mask greenboot-healthcheck.service
